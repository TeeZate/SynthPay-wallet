# ── Wallet STAGING environment ────────────────────────────────────────────────
# Set these as Vercel Environment Variables on the staging deployment.

VITE_API_BASE_URL=https://trustledger-staging.up.railway.app

# PostHog — use the same project; staging traffic will be tagged
VITE_POSTHOG_KEY=phc_REPLACE_ME
VITE_POSTHOG_HOST=https://us.i.posthog.com
